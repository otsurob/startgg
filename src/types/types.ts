export type Pools = {
    letter : string,
    poolNums : {
        poolNum : string,
        players : string[]
    }[]
};

export type poolResponses = {
    pool : string,
    gamerTag : string
}[];